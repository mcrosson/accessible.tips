# AGENTS.md — accessible.tips

Project-level guidance for agents working in this repo. (Operator workflow /
process rules live in the global AGENTS.md; this file is about THIS project's
facts and conventions.)

## Read first

- **`.agents/orientation.md`** — the project map: purpose, content shape,
  front-matter contracts, the rendering/search pipeline, build & deploy, and
  high-risk maintenance points. Read it before touching anything. It is the
  authoritative detailed reference; this file is the routing layer.
- **`.agents/skills/`** — testing skills (see below).

## Keeping `.agents/orientation.md` fresh

`.agents/orientation.md` is the authoritative map but drifts after structural
change. It is (re)generated by the `orientation` skill
(`.agents/skills/orientation/`), which stamps a `validated-at` header — that
header is the signal of how current the brief is.

- **Invocation is the operator's.** The operator decides when to re-run the
  `orientation` skill; an agent does not regenerate the brief on its own
  initiative.
- **Nudge after substantial, finalized change.** When a completed body of work
  changes load-bearing structure — a new content section or taxonomy, the
  build/deploy pipeline (`bin/*.sh`, Netlify), a stack pin, a new shortcode or
  template pattern in `themes/hugo-xmin/`, or the hard requirements below — flag
  that the brief is now likely behind and remind the operator to re-run the
  `orientation` skill. Don't let it silently go stale.
- **Not for routine edits.** Ordinary content work (a new tip, copy fixes, SCSS
  value tweaks) does not warrant a re-orientation — only changes to the
  load-bearing facts the brief captures.

## What this site is

A Hugo static site: a zettelkasten-style accessibility knowledge base
(`content/tips/`, `content/anecdotes/`, `content/faq/`, `content/news/`). Built
on a heavily customized `themes/hugo-xmin/` plus a `hugo-admonitions` component.
Deployed via Netlify. Licensed CC BY-SA 4.0.

## Hard requirements (site-wide, non-negotiable)

- **Accessibility: WCAG AA minimum, AAA wherever it's free or cheap — across the
  entire site.** This is a hard requirement, not a target. The site is an
  accessibility resource, so its own a11y IS the product, not polish. Do not
  regress AA; take AAA when it costs little or nothing. (Template-level specifics
  — alt text, ARIA labels, live regions, focus visibility, `u-visually-hidden`,
  admonition markup — are under House rules.)
- **Low-bandwidth: stay maximally usable on slow or intermittent connections.**
  Keep page size down where possible. The build already minifies (`--minify`);
  preserve the existing weight-conscious patterns — responsive WebP via the
  `figure` shortcode (400/800/1200) and lazy-loaded runtimes — and don't add
  heavy assets or client bundles without need.

## Stack pins — do not change

- Hugo 0.155.3 extended, vendored at `bin/hugo-0.155.3` — NOT on PATH; call it
  by path.
- `bin/pagefind` vendored.
- No Node in the build (SCSS via Hugo Pipes/libsass, JS via Hugo's built-in
  esbuild). Never add `package.json` / `node_modules` / `@playwright/test`.
- Treat `themes/hugo-xmin/` and `themes/hugo-admonitions/` as application code,
  not pristine upstream (the in-theme README is stale).

## Build / serve / test

- `bin/build.sh [outdir]` — Hugo (`--gc --minify`) then Pagefind. Defaults to
  `public/`; pass a scratch dir for local builds.
- `public/` is gitignored STALE OUTPUT — never hand-edit, never regenerate as a
  deliverable.
- `bin/dev.sh [port]` — live-reload; includes drafts (`-D`); `/search/` is INERT
  (Pagefind never runs).
- `bin/preview.sh [port|branch]` — production-like + searchable over HTTP;
  drafts excluded; builds with the production baseURL baked in.
- `bin/end-preview.sh [port]` — idempotent teardown.
- Verify search / facets / equivalence over HTTP via `preview.sh` — `dev.sh` is
  silent and `file://` is misleading.
- Aliases / canonical / absolute URLs: build with an explicit local `--baseURL`
  (see the `site-browser` skill) or following an alias navigates to production.

## Testing skills (`.agents/skills/`)

All Playwright-driven; all REQUIRE a vision-capable model.

- **site-browser** — capability layer: serve + drive + artifacts + teardown.
  The other testing skills build on this.
- **smoke-test** — breakage assertions (search, equivalence, assets/SRI,
  Plausible, RSS, console cleanliness, internal links, aliases). Run after any
  change to templates / SCSS / JS / `config.toml` / search / taxonomy.
- **a-b-compare** — two-version side-by-side (A/B, before/after, two branches).

Test artifacts go under `.scratch/test-artifacts/` (gitignored).

## Conventions that bite

- New pages start `draft = true`; use `bin/dev.sh` to see draft content
  (`preview.sh` / `build.sh` exclude drafts).
- Don't add a hand-written `## Changelog` — `single.html` auto-renders it from
  front matter; the "Updated" masthead tracks the changelog, not GitInfo.
- Anecdotes need BOTH `categories: [anecdote]` AND `tags: [anecdote]`; FAQ is
  tagged `faq` only; news is intentionally untagged.
- Images: use the `figure` shortcode (responsive WebP + Tobii lightbox); use
  page bundles (`dir/index.md`) when a page owns assets.
- The contributing FAQ mentions `posts/` — the real blog is `news/`.
- Each section has a `_test/` fold with a likely-draft `index.md` — check before
  assuming a section is empty.
- Goldmark has `unsafe = true`: raw HTML in pages renders AND is indexed by
  Pagefind.

## House rules

- Preserve accessibility details when editing templates: alt text, ARIA labels,
  live regions, focus visibility, the `u-visually-hidden` helper, admonition
  markup.
- Keep `hugo-xmin` and `hugo-admonitions` in lock-step when touching admonitions.
- Use the vendored binaries; assume no system Hugo or Node toolchain.
- Do not reintroduce Decap / Static CMS (removed 2024-07).
- Git is the operator's — never run git write operations; never prompt about
  commits.

## High-risk files (touch with care; verify after)

`config.toml`, and in `themes/hugo-xmin/`: `header.html`, `page_meta.html`,
`single.html`, `search.html`, `search-page.js`, `corrector.js`,
`theme-toggle.html`, `style.css`. (Full rationale in `.agents/orientation.md` →
"High-risk maintenance points".)
